name: ZDownload m3u8
on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      Url:
        description: 'Download Url'
        required: false
        default: 'false'
#   watch:
#     types: started
env:
  FILE_PATH: 'downloads'
jobs:
  m3u8download:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false 
        fetch-depth: 0 
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
        
    - name: install prerequisites
      run: |
        python -m pip install --upgrade pip wheel
        pip install install requests cloudscraper pymongo pymongo[srv] pymongo[tls] certifi
    - uses: FedericoCarboni/setup-ffmpeg@v1
    - name: Downloading
      id: downloading
      run: |
        url=$(cat url)
        filename=${url##*##}
        download=${url%##*}
        downloadurl=$(python get_m3u8.py $download)
        mkdir video
        ffmpeg -user_agent "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/92.0.4515.159 Safari/537.36" -i $downloadurl video/$filename
        echo "::set-output name=path::video/$filename"
        echo "::set-output name=filename::$filename"
       
    - uses: ykxVK8yL5L/add-to-web3@v1.3
      id: web3
      if: steps.downloading.outputs.filename != '' && !cancelled()
      with:
        web3_token: ${{ secrets.WEB3_STORAGE_TOKEN }}
        path_to_add: ${{ steps.downloading.outputs.path }} 
        file_name: ${{ steps.downloading.outputs.filename }} 
        wrap_with_directory: true
    - run: echo ${{ steps.web3.outputs.url }}
    - name: Save Data
      if: steps.web3.outputs.url != '' && !cancelled()
      run: |
        python save.py --con="${{ secrets.DB_CONNECT }}" --name="${{ steps.downloading.outputs.filename }}" --url="${{ steps.web3.outputs.url }}/${{ steps.downloading.outputs.filename }}" --cid="${{ steps.web3.outputs.cid }}"
